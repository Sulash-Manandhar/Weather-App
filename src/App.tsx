import Dashboard from "./Dashboard";
// require("dotenv").config();
// console.log(process.env.userId);
const App = () => {
  return (
    <div>
      <Dashboard />
    </div>
  );
};

export default App;
